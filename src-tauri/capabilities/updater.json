{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "enables the updater plugin",
  "windows": ["main"],
  "permissions": [
    "updater:default",
    "process:allow-restart"
  ]
}
